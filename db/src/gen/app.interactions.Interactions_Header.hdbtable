-- generated by cds-compiler version 2.10.4
COLUMN TABLE app_interactions_Interactions_Header (
  ID INTEGER NOT NULL,
  PARTNER NVARCHAR(10),
  LOG_DATE SECONDDATE,
  BPCOUNTRY_code NVARCHAR(3),
  PRIMARY KEY(ID)
) WITH ASSOCIATIONS (
  MANY TO MANY JOIN app_interactions_Interactions_Items AS ITEMS ON (ITEMS.INTHeader_ID = ID),
  MANY TO ONE JOIN sap_common_Countries AS BPCOUNTRY ON (BPCOUNTRY.code = BPCOUNTRY_code)
)